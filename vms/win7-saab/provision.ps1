# provision.ps1 -- bootstraps the Win7-SAAB VM with vendor diagnostic stack.
# Run inside the guest. Requires vendor/ ISOs to be present in the host
# (synced into /vendor inside the guest).

Write-Host "[*] Win7-SAAB provisioning placeholder."
Write-Host "    Phase-2: install GDS2, Tech2Win, GlobalTIS from /vendor/isos."
Write-Host "    Phase-2: install J2534 PassThru DLL from /vendor/drivers/j2534."

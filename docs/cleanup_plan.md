# Cleanup Plan

## Goals

- Remove legacy artifacts.
- Keep repo deterministic and reproducible.

## Steps

1. Remove legacy package src/saab_suite/ after migration is committed.
2. Purge __pycache__, *.pyc, *.pyo, *.bak.
3. Remove obsolete archives that are regenerated by tooling.
4. Normalize script permissions for tools/, bin/, packaging/.
5. Ensure .gitignore covers runtime and build artifacts.

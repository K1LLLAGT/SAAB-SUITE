# Windows companion installer

Targets the Win7-SAAB and WinXP-SAAB VMs that host the
vendor diagnostic stack (GDS2, Tech2Win, GlobalTIS, J2534 Toolbox).
Requires Inno Setup 6+ on a Windows host to build.
